# JAX add-on environment layered on top of requirements.txt.
# Install requirements.txt first, then this file.
#
# For local CPU environments, this file is usually enough.
# For GPU/HPC systems, install the platform-specific jax/jaxlib wheels first,
# then install the remaining dependencies from this file.

jax>=0.4.30
jaxlib>=0.4.30
optax>=0.2.3
numpy>=1.23,<2
scipy>=1.10,<1.13
matplotlib>=3.6
pyyaml>=6.0
tqdm>=4.64
